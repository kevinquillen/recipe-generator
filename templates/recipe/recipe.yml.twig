name: '{{ recipe_name }}'
description: '{{ recipe_description }}'
type: '{{ recipe_type }}'

install:
{% for module in modules %}
  - {{ module }}
{% endfor %}

{% if config %}
config:
  import:
  actions:
{% endif %}
