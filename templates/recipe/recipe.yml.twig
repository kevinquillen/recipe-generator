{{ machine_name }}.settings:
  type: config_object
  label: '{{ name }} API settings'
  mapping:
    api_key:
      type: string
      label: 'The API key.'
    hostname:
      type: string
      label: 'The hostname or base URI of the API.'
